{
  "name": "app",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "tsc && vite build",
    "verify:voicings": "node src/verify-voicings.mjs",
    "verify:verdicts": "node src/verify-verdicts.mjs",
    "verify:strumcam": "node src/verify-strumcam.mjs",
    "preview": "vite preview",
    "tauri": "tauri",
    "verify:songparse": "node src/verify-songparse.mjs"
  },
  "dependencies": {
    "@tauri-apps/api": "^2",
    "@tauri-apps/plugin-opener": "^2"
  },
  "devDependencies": {
    "@tauri-apps/cli": "^2",
    "vite": "^6.0.3",
    "typescript": "~5.6.2"
  }
}
