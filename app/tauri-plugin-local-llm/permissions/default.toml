"$schema" = "schemas/schema.json"

[default]
description = "Allows checking on-device model availability and running on-device chat completions."
permissions = [
    "allow-availability",
    "allow-chat",
]
